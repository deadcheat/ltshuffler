{
  "name": "ltshuffler",
  "version": "1.0.0",
  "description": "",
  "main": "component.js",
  "watch": {
    "build": {
      "patterns": [
        "components"
      ],
      "extensions": "html",
      "quiet": false
    }
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "npm-watch",
    "build": "svelte compile --format iife components/Component.html > dist/Component.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "npm-watch": "^0.2.0",
    "watch": "^1.0.2"
  }
}
